# Cursor Adapter

Read `AGENTS.md` first.

## Minimal behavior

- Read `wiki/index.md` first.
- Follow the relevant project page.
- Use `./cli/wikictl sync` at the end of the task.

## Keep it simple

Cursor-specific files should stay thin. Do not duplicate the workflow here.
