# Claude Adapter

Read `AGENTS.md` first.

## Minimal behavior

- Read `wiki/index.md` before starting.
- Open the relevant `wiki/projects/*.md` page.
- Touch `raw/` only as an immutable source layer.
- Use `./cli/wikictl sync` at the end of the session.

## Keep it simple

This file is intentionally thin. The shared contract lives in `AGENTS.md`.
