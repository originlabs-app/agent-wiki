# Codex Adapter

Read `AGENTS.md` first.

## Minimal behavior

- Use the same read order as every other agent.
- Keep work in the repo; keep sources immutable in `raw/`.
- Use `./cli/wikictl sync` when the session ends.

## Keep it simple

This is a thin adapter only. The shared contract lives in `AGENTS.md`.
